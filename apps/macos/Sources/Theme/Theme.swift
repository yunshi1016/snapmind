import SwiftUI

/// SnapMind 设计 token（移植自 Windows 版品牌色）。
enum Theme {
    /// 品牌主色（紫）#7C6CF0
    static let brand = Color(red: 124.0 / 255, green: 108.0 / 255, blue: 240.0 / 255)
    /// 深底 #101014
    static let bg = Color(red: 16.0 / 255, green: 16.0 / 255, blue: 20.0 / 255)
}
